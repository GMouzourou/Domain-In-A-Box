# Single node on purpose: macvlan "bridge" mode only bridges interfaces that
# share a master, so the DC and the test client must land on the same node for
# DHCP broadcast to reach across.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
